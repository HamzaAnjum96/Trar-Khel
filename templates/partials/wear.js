// wear.js — LAYER 4 (wear): punch holes, fold stress, edge abrasion, tag wear.
//
// Sprint 3 placeholder: returns an empty layer so pages render completely.
// Sprint 4 replaces this with positional wear primitives (top-left punch holes,
// axis fold stress, edge darkening) driven by the spec + seed.

export function renderWear(/* spec, ctx */) {
  return "";
}
