// marks.js — LAYER 3 (mark): stamps, seals, daters, cancellations.
//
// Sprint 3 placeholder: returns an empty layer so pages render completely.
// Sprint 4 replaces this with the imperfection engine (seeded placement in one
// quadrant, five ink failure modes, mix-blend-mode into the paper).

export function renderMarks(/* spec, ctx */) {
  return "";
}
